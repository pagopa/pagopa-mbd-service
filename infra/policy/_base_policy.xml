<policies>
  <inbound>
    <base/>
    <choose>
      <when condition="@(!((string)context.Request.Headers.GetValueOrDefault("X-Orginal-Host-For","")).Equals("api.prf.platform.pagopa.it") && !((string)context.Request.OriginalUrl.ToUri().Host).Equals("api.prf.platform.pagopa.it"))">
        <!-- rate limit by subscription key -->
        <rate-limit-by-key calls="300" renewal-period="10" counter-key="@(context.Subscription.Key)" increment-condition="@(context.Response.StatusCode != 429)" remaining-calls-header-name="x-remaining-calls-per-10-secs" />
      </when>
    </choose>

    <!-- Calling Authorizer's fragment -->
    <set-variable name="application_domain" value="mbd" />
    <set-variable name="metadata" value="no-metadata" />
    <choose>
        <!-- Making sure that will exclude all APIs that does not include CI fiscal code -->
        <when condition="@(context.Request.MatchedParameters.ContainsKey("organization-fiscal-code"))">
          <set-variable name="authorization_entity" value="@(context.Request.MatchedParameters["organization-fiscal-code"])" />
          <include-fragment fragment-id="authorizer" />
        </when>
    </choose>
  </inbound>
  <outbound>
    <base/>
  </outbound>
  <backend>
    <base/>
  </backend>
  <on-error>
    <base/>
  </on-error>
</policies>